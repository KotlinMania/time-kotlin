# Port targets — `time-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `time` that codex-rs uses, and therefore the minimum subset that `time-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-154] (rank `20`, tier `direct`, status `To Do`)

**Cargo package name**: `time`
**Rust module name (in `use` statements)**: `time`

## Summary

- **6** distinct symbols imported by codex-rs
- **19** total `use` statements
- **7** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `time::OffsetDateTime`

- **7 `use` site(s)** across **4 codex member crate(s)**: `codex-app-server`, `codex-app-server-transport`, `codex-network-proxy`, `codex-rollout`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/auth.rs:21` (`codex-app-server-transport`) — `use time::OffsetDateTime`
- `app-server/tests/suite/v2/connection_handling_websocket.rs:32` (`codex-app-server`) — `use time::OffsetDateTime`
- `network-proxy/src/runtime.rs:31` (`codex-network-proxy`) — `use time::OffsetDateTime`
- `rollout/src/list.rs:12` (`codex-rollout`) — `use time::OffsetDateTime`
- `rollout/src/recorder.rs:19` (`codex-rollout`) — `use time::OffsetDateTime`
- `rollout/src/session_index.rs:34` (`codex-rollout`) — `use time::OffsetDateTime`
- `rollout/src/tests.rs:15` (`codex-rollout`) — `use time::OffsetDateTime`

### `time::format_description::FormatItem`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-rollout`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rollout/src/list.rs:14` (`codex-rollout`) — `use time::format_description::FormatItem`
- `rollout/src/recorder.rs:20` (`codex-rollout`) — `use time::format_description::FormatItem`
- `rollout/src/tests.rs:17` (`codex-rollout`) — `use time::format_description::FormatItem`

### `time::format_description::well_known::Rfc3339`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-rollout`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rollout/src/list.rs:15` (`codex-rollout`) — `use time::format_description::well_known::Rfc3339`
- `rollout/src/recorder.rs:21` (`codex-rollout`) — `use time::format_description::well_known::Rfc3339`
- `rollout/src/session_index.rs:35` (`codex-rollout`) — `use time::format_description::well_known::Rfc3339`

### `time::macros::format_description`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-rollout`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rollout/src/list.rs:16` (`codex-rollout`) — `use time::macros::format_description`
- `rollout/src/recorder.rs:22` (`codex-rollout`) — `use time::macros::format_description`
- `rollout/src/tests.rs:18` (`codex-rollout`) — `use time::macros::format_description`

### `time::PrimitiveDateTime`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-rollout`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rollout/src/list.rs:13` (`codex-rollout`) — `use time::PrimitiveDateTime`
- `rollout/src/tests.rs:16` (`codex-rollout`) — `use time::PrimitiveDateTime`

### `time::Duration`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-rollout`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `rollout/src/tests.rs:14` (`codex-rollout`) — `use time::Duration`
